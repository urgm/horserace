<h1>予想画面</h1>

<table>
  <tr>
    <th>レース名</th>
    <th>馬名</th>
    <th>馬番</th>
    <th>予想印</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tips.each do |tip| %>
  <tr>
    <td><%= tip.race_id %></td>
    <td><%= tip.user_name %></td>
    <td><%= tip.horse_no %></td>
    <td><%= tip.rank %></td>
    <td><%= link_to 'Show', tip %></td>
    <td><%= link_to 'Edit', edit_tip_path(tip) %></td>
    <td><%= link_to 'Destroy', tip, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to '新規予想', new_tip_path %>
